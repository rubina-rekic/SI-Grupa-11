# Decision Log

Ovaj dokument služi za evidentiranje važnih projektnih, zahtjevnih, arhitektonskih, tehničkih i procesnih odluka. Svrha dnevnika je da pokaže svjesno donošenje odluka i praćenje njihovog toka unutar tima.

| ID | Datum | Naziv odluke | Opis problema / pitanja | Razmatrane opcije | Odabrana opcija | Razlog izbora | Posljedice odluke | Status |
|:---|:---:|:---|:---|:---|:---|:---|:---|:---|
| DEC-001 | 2026-04-25 | Toast biblioteka za korisničke notifikacije | US-03 zahtijeva feedback o statusu kreiranja računa (uspjeh, greška servera). Potrebna je React-kompatibilna biblioteka za toast notifikacije. | `sonner`, `react-hot-toast`, `react-toastify` | `sonner` | Najmanji bundle, moderna API s Promise-based toastovima, native podrška za loading/success/error stanja u jednom pozivu, dobra TypeScript podrška. `react-toastify` je veći i stariji API; `react-hot-toast` nema Promise API. | Sve toast notifikacije u projektu koriste `sonner`. Izmjena biblioteke zahtijevala bi refaktoring svih pozivnih mjesta. | Aktivna |
| DEC-002 | 2026-04-25 | Biblioteka za hashiranje lozinki | Potrebno je sigurno hashiranje lozinki pri kreiranju korisničkog računa. TechnicalSetup navodi ASP.NET Identity, ali puni Identity framework uvodi kompleksnost (Identity DbContext, migracije, cookie middleware) koja nije potrebna uz JWT. | Puni `ASP.NET Identity` framework, `BCrypt.Net-Next` direktno, `Microsoft.AspNetCore.Identity.IPasswordHasher` bez punog frameworka | `BCrypt.Net-Next 4.1.0` direktno u BLL sloju | BCrypt je industrijski standard za hashiranje lozinki s ugrađenim salt-om i cost factorom. Puni ASP.NET Identity uvodi previše overhead-a za projekat koji već ima vlastiti User entitet i JWT. Ovo je lakše testirati i manje vezano za infrastrukturu. | Hashiranje je isključivo odgovornost BLL sloja. Ako se ikada pređe na ASP.NET Identity, migracija hashova nije direktna. | Aktivna |
| DEC-003 | 2026-04-25 | Arhitektura HTTP klijenta na frontendu | Frontend treba komunicirati s backendom. Direktno korištenje Axiosa po komponentama bi raspršilo konfiguraciju (base URL, JWT headeri, error handling) po cijeloj aplikaciji. | Direktno koristiti Axios svuda, centralizovani wrapper sa interceptorima, `fetch` API | Centralizovani `httpClient` wrapper oko Axios instance s JWT interceptorom | Jedan interceptor automatski dodaje `Authorization: Bearer <token>` na sve zahtjeve i centralizuje error handling. Frontend komponente ne trebaju znati ništa o autentifikaciji. Usklađeno s postavljenom `infrastructure/` slojevnom arhitekturom. | Svi API pozivi prolaze kroz `httpClient`. Axios je jedina direktna zavisnost — ostatak koda nije vezan za specifičnu HTTP biblioteku. | Aktivna |
| DEC-004 | 2026-04-25 | Ignorisanje appsettings.Development.json u verzionisanju | `appsettings.Development.json` sadrži connection string s kredencijalima za lokalnu PostgreSQL bazu. Commitanje ovog fajla bi izložilo lozinke u repozitoriju. | Commitati fajl (svi imaju iste lokalne kredencijale), ignorisati u .gitignore i koristiti .NET User Secrets, ignorisati i dokumentovati ručni setup | Ignorisati u `.gitignore`; svaki developer kreira lokalno | Kredencijali ne smiju biti u repozitoriju čak i za dev okruženje — navika koja štiti i od slučajnog commitanja produkcijskih tajni. .NET User Secrets su alternativa, ali dodaju overhead za tim početnika. | Novi developer mora ručno kreirati `appsettings.Development.json`. Potrebno dokumentovati u README setupu. | Aktivna |
| DEC-005 | 2026-04-25 | Uloga i MustChangePassword fiksirani u servisnom sloju | Pri kreiranju računa poštara, uloga i zastavica za promjenu lozinke moraju biti postavljene konzistentno bez obzira na to koji dio sistema inicira kreiranje. Pitanje: treba li ovo biti UI izbor ili poslovna pravila? | Uloga i flag se prosljeđuju kroz API zahtjev (fleksibilno), hardkodirani su u kontroleru, hardkodirani su u servisu (BLL) | Hardkodirano u `UserService.CreateAsync` — `Role = "PostalWorker"`, `MustChangePassword = true` | Poslovna pravila (ko je Poštar, da mora promijeniti lozinku) ne smiju zavisiti od UI-a ili API pozivatelja. Servis je jedina tačka koja kreira Poštare — ovaj use case ne kreira druge uloge. | Svaki buduće kreiranje korisnika s drugom ulogom (Dispečer, Admin) zahtijeva zaseban servisni metod ili komandu. Nema rizika od slučajnog dodjeljivanja pogrešne uloge kroz API. | Aktivna |
| DEC-006 | 2026-04-25 | Rukovanje konfliktima pri duplikatima emaila/korisničkog imena | Kada admin pokuša kreirati korisnika s već postojećim emailom ili korisničkim imenom, sistem mora vratiti smislen odgovor. Pitanje: gdje i kako detektovati i prijaviti duplikat? | Provjera u kontroleru (direktno), provjera u servisu s result tipom (npr. `CreateUserResult`), provjera u servisu s iznimkom | `InvalidOperationException` u servisnom sloju, uhvaćena u kontroleru koji vraća `409 Conflict` s porukom | Servis je odgovoran za poslovnu logiku — uključujući jedinstvenost. Result tip je čišći, ali uvodi dodatnu apstrakciju koja nije opravdana u ovoj fazi. Exception-based pristup je precizan i jednostavan za tim početnika; poruka iznimke direktno opisuje razlog konflikta. | Kontroler mora sadržavati `try/catch` za `InvalidOperationException`. Ako se u budućnosti uvede više razloga za odbijanje, može se uvesti domenski `DomainException` ili result tip. | Aktivna |
| DEC-007 | 2026-04-25 | Validacija lozinke — klijentska i serverska | NFR-01 propisuje minimalnu jačinu lozinke. Pitanje: gdje primijeniti pravila — samo frontend, samo backend, ili obje strane? | Samo Zod na frontendu, samo data annotations na backendu, dupla validacija na obje strane | Dupla validacija: Zod schema na frontendu + data annotations na `CreateUserRequest` | Frontend validacija daje trenutni feedback korisniku bez mrežnog poziva. Backend validacija štiti API od direktnih poziva koji zaobilaze UI (curl, Postman). Obje strane implementuju ista pravila iz NFR-01. | Svaka promjena pravila mora biti ažurirana na obje strane. Prihvatljivo jer su pravila jednostavna i stabilna (NFR-01). | Aktivna |
| DEC-008 | 2026-04-25 | Indikator jačine lozinke — kriteriji i nivoi | US-02 zahtijeva vizuelni indikator jačine lozinke. Pitanje: koliko nivoa i koji kriteriji? | 3 nivoa (Slaba/Srednja/Jaka) s 3 NFR kriterija, 4 nivoa s bonus kriterijem za duže lozinke | 4 nivoa (Slaba/Srednja/Dobra/Jaka): dužina ≥8, veliko slovo, broj (NFR kriteriji) + bonus: dužina ≥12 | 4 nivoa daju finiji feedback i potiču korisnika da odabere jaču lozinku od minimuma. Bonus kriterij ne mijenja minimalne zahtjeve — lozinka s 3/4 kriterija je validna, ali indikator pokazuje da može biti jača. | Indikator je isključivo UX alat — validacija se oslanja na Zod schema (3 NFR kriterija). Neslaganje između indikatora i validacije nema. | Aktivna |
| DEC-009 | 2026-04-25 | Strategija feedback-a po tipu odgovora servera | Forma za kreiranje korisnika može dobiti tri tipa odgovora: 201 (uspjeh), 409 (poslovni konflikt), 5xx/network (neočekivana greška). Pitanje: kako prikazati svaki tip korisniku? | Svi odgovori kao toast, svi kao inline poruke, miješano po tipu | Miješana strategija: 201 → toast uspjeh; 409 → inline greška na konkretnom polju; 5xx/network → toast greška | Toast za uspjeh nestaje sam i ne blokira UI. Inline greška za 409 daje precizan kontekst (koje polje je problem). Toast za serversku grešku jer nije vezana za konkretno polje — prikladnija je globalna notifikacija. | Svaka nova forma u sistemu treba primjenjivati ovu strategiju konzistentno. Uniformnost smanjuje kognitivno opterećenje za korisnika. | Aktivna |
| DEC-010 | 2026-04-25 | Reset forme nakon uspješnog kreiranja umjesto redirecta | Nakon uspješnog kreiranja poštara, sistem treba odlučiti što se dešava s formom. Pitanje: redirect ili reset? | Redirect na listu korisnika, redirect na profil novokreiranog korisnika, reset forme na istoj stranici | Reset forme (`reset()` iz react-hook-form) na istoj stranici | Admin često kreira više poštara zaredom — reset je brži od navigacije tamo-natrag. Lista korisnika nije još implementirana (kasniji sprint). Toast potvrđuje uspjeh, a prazna forma signalizira spreman za unos. | Kada lista korisnika bude implementirana, može se razmotriti redirect kao bolja opcija za UX. | Aktivna |
| DEC-011 | 2026-04-25 | Dev PostgreSQL na portu 5433 umjesto 5432 | Tim od 7 članova ima različita razvojna okruženja — neki imaju lokalni PostgreSQL na defaultnom portu 5432 (što je uzrokovalo auth grešku pri migraciji). Pitanje: koji port koristiti za dev Docker kontejner? | Zadržati port 5432 i tražiti od svih da ugase lokalni PostgreSQL, koristiti port 5433 za Docker, koristiti env varijablu za port | Port 5433 za Docker dev kontejner; connection string ažuriran u `appsettings.Development.json` | Ne zahtijeva od članova tima da mijenjaju ili gase lokalni PostgreSQL. Svako može imati lokalni i Docker PostgreSQL paralelno. Port 5433 je dovoljno blizu defaultu da je intuitivan, a dovoljno različit da nema konflikta. | Svi članovi tima moraju koristiti port 5433 u `appsettings.Development.json`. Dokumentovano u `PROJEKAT/README.md` s templateom za konfiguraciju. | Aktivna |
| DEC-012 | 2026-04-25 | `Created()` umjesto `CreatedAtAction()` u UsersController | `CreatedAtAction(nameof(GetByIdAsync), new { userId = user.Id }, response)` bacao je 500 grešku ("No route matches the supplied values") jer ASP.NET Core ne može razriješiti rutu — ime parametra u rutetemplate-u (`{userId:guid}`) mora se tačno podudarati s ključem u anonimnom objektu. Pitanje: koristiti `CreatedAtAction` s ispravnim ključem ili alternativni pristup? | Ispraviti ključ u anonimnom objektu (`userId` → ispravan naziv), koristiti `CreatedAtRoute`, koristiti `Created()` s ručno konstruisanim URL-om | `Created($"/api/users/{user.Id}", response)` — direktna string interpolacija URL-a | Eliminira refleksiju i mogućnost desinhronizacije između ključa u anonimnom objektu i parametra rute. Ovaj API nema HATEOAS navigaciju niti verzionisanje ruta gdje bi `CreatedAtAction` donio prednost. String interpolacija je transparentna i ne može baciti `InvalidOperationException` u runtime. | Ako se ruta za `GetByIdAsync` ikad promijeni, URL u `Created()` mora se ažurirati ručno. Prihvatljivo dok nema više verzija API-ja. | Aktivna |
| DEC-013 | 2026-04-27 | Zaštita login endpointa od brute-force napada | Nakon implementacije login funkcionalnosti, potrebno je spriječiti brzo pogađanje lozinke (brute-force). | Samo backend lockout, frontend cooldown, kombinacija oba pristupa | Kombinacija: backend lockout nakon 5 pokušaja + frontend cooldown 3 sekunde | Backend osigurava stvarnu zaštitu (zaključavanje računa), frontend cooldown poboljšava UX i usporava napade bez dodatnih zahtjeva prema serveru | Potrebno održavati konzistentnost između frontend i backend logike pokušaja prijave | Aktivna |
| DEC-014 | 2026-04-27 | Obavezna promjena lozinke pri prvom loginu | Novi korisnici dobijaju inicijalnu lozinku koja nije sigurna za dugoročnu upotrebu | Opcionalna promjena lozinke, prisilna promjena putem UI-a, backend enforcement | Backend enforcement putem MustChangePassword flag-a + frontend redirect | Sigurnosno najispravnije rješenje — korisnik ne može koristiti sistem dok ne promijeni lozinku | Potrebna dodatna logika za blokadu navigacije i poseban ekran | Aktivna |
| DEC-015 | 2026-04-27 | Backend kontrola statusa promjene lozinke | Potrebno odlučiti gdje se čuva informacija da li korisnik mora promijeniti lozinku | Samo frontend (localStorage), samo backend, kombinacija | Backend kao izvor istine (MustChangePassword u bazi) | Frontend može biti manipulisan; backend garantuje sigurnost i konzistentnost | Frontend mora svaki put čitati stanje iz API odgovora | Aktivna |
| DEC-016 | 2026-04-27 | Poseban API endpoint za promjenu lozinke | Nakon prvog login-a korisnik mora promijeniti lozinku | Promjena lozinke kroz login endpoint, poseban endpoint | Poseban endpoint `/api/users/change-password` | Odvajanje odgovornosti — login služi samo za autentifikaciju, promjena lozinke je zaseban proces | Potrebno održavati dodatni endpoint i validaciju | Aktivna |
| DEC-017 | 2026-04-28 | Odjava kroz brisanje JWT tokena bez backend invalidacije | JWT je stateless — server ne može invalidirati token. Pitanje: da li implementirati backend blacklistu tokena ili samo brisati token na frontendu? | Backend blacklista tokena u bazi, Redis cache za invalidaciju, samo brisanje iz localStorage na frontendu | Brisanje tokena iz localStorage na frontendu + prazan logout endpoint | Za MVP scope token kratko živi i rizik zloupotrebe je prihvatljiv. Backend blacklista zahtijeva dodatnu infrastrukturu koja nije opravdana u ovoj fazi. | Ako token istekne prirodno prije odjave, korisnik je već efektivno odjavljen. Post-MVP može se uvesti refresh token strategija s blacklistom. | Aktivna |
| DEC-018 | 2026-04-29 | Čuvanje `MustChangePassword` u server sesiji umjesto ponovnog čitanja iz baze | `GET /api/users/current-user` nije imao pristup `MustChangePassword` bez dodatnog upita u bazu, jer endpoint rekonstruiše korisnika iz sesijskih podataka (UserId, UserRole, Username, Email). Pitanje: čitati iz baze na svakom pozivu ili proširiti sesiju? | Uvijek čitati `MustChangePassword` iz baze po `UserId`; čuvati flag u sesiji pri prijavi i ažurirati ga pri promjeni | Čuvati `MustChangePassword` u sesiji: `Session.SetString("MustChangePassword", ...)` pri `LoginAsync`, osvježiti na `False` nakon `ChangePasswordAsync`, čitati u `GetCurrentUser` | Konzistentno s načinom na koji se čuvaju ostali korisnički podaci (Role, Email, Username). Izbjegava se dodatan DB upit na svakom HTTP zahtjevu. Sesija je single source of truth za trajanje sesije — nema rizika od desinhronizacije jer se flag može promijeniti samo unutar iste sesije. | `MustChangePassword` u sesiji ostaje `True` dok ga korisnik ne promijeni unutar iste sesije. Nova prijava uvijek pročita vrijednost iz baze i osvježi sesiju. | Aktivna |
| DEC-019 | 2026-04-29 | Konsolidacija logout logike u `useAuth` — brisanje `useLogout.ts` | Postojala su dva odvojena mehanizma za odjavu: `useAuth.logout` (koristio se u `Layout.tsx`) i `useLogout.ts` hook (nikad importovan nigdje). Pitanje: zadržati oba, odabrati jedan, ili spojiti? | Zadržati oba hooka i koristiti `useLogout.ts` u `Layout`; izbrisati `useLogout.ts` i poboljšati `useAuth.logout`; prebaciti svu logout logiku u `useLogout.ts` | Izbrisati `useLogout.ts`, dodati `replace: true` i toast u `useAuth.logout` | `useLogout.ts` bio dead code — nikad korišten. Jedan izvor istine za logout logiku sprječava buduće divergencije. `useAuth` već posjeduje kontekst korisnika i navigate — prirodno mjesto za logout. | Sve komponente koje trebaju logout koriste `useAuth().logout`. Nema posebnog hooka za odjavu. | Aktivna |
| DEC-020 | 2026-04-29 | Tematika UI dizajna — naval blue + amber umjesto generic purple | Aplikacija koristila generički purple gradijent (`#667eea`→`#764ba2`) bez veze s domenom. Pitanje: zadržati neutralne boje ili uvesti tematiku vezanu za poštansku službu? | Zadržati neutral/purple gradijent; uvesti zelenu/smeđu tematiku; uvesti naval blue + amber | Naval blue (`#1b3a5c`) kao primarna boja + amber (`#d4870a`) kao akcent | Naval blue je boja karakteristična za poštanske uniforme i službene institucije u regiji. Amber/žuta asocira na poštanske sandučiće i vozila. Kombinacija je profesionalna, ne odudara od PostRoute naziva i jasno je vezana za domenu. | Sve buduće komponente trebaju koristiti CSS varijable `--navy` i `--amber` definisane u `layout.css`. Promjena teme zahtijeva izmjenu samo varijabli. | Aktivna |
| DEC-021 | 2026-04-29 | `noValidate` na svim formama + standardni `autoComplete` atributi | React Hook Form i custom validacija upravljaju provjerom unosa. Browser-nativna validacija (`type="email"` popup itd.) može se pokrenuti paralelno i dati nekonzistentno iskustvo. Pitanje: osloniti se na browser validaciju ili je isključiti? | Osloniti se na browser validaciju; isključiti je s `noValidate`; koristiti hibridni pristup | `noValidate` na svim formama; `autoComplete` atributi po WHATWG standardu na svim inputima | `noValidate` daje potpunu kontrolu nad UX greški — svi se prikazuju konzistentno ispod polja. `autoComplete` atributi (email, current-password, new-password, given-name, family-name, username) poboljšavaju UX i neophodne su za password managere i pristupačnost. | Svaka nova forma mora imati `noValidate` i odgovarajuće `autoComplete` atribute. Accessibility audit (label/htmlFor) obavezan pri svakom novom formularu. | Aktivna |
| DEC-022 | 2026-05-03 | Filtriranje administratora iz liste korisnika | Lista korisnika na /admin/users treba prikazivati samo poštare, ne administratore. Pitanje: filtrirati na frontendu ili backendu? | Filtriranje na backendu (poseban endpoint), filtriranje na frontendu | Filtriranje na frontendu kroz filter(u => u.role !== "Administrator") | Brže rješenje bez dodatnog endpointa; lista korisnika je kratka pa nema performansnih problema. Post-MVP može se uvesti poseban endpoint ako baza poraste. | Endpoint vraća sve korisnike — ako se negdje drugdje koristi isti endpoint, administratori će biti vidljivi. | Aktivna |
| DEC-023 | 2026-05-06 | Obrisati MailboxLocationForm.tsx koja koristi neinstaliran react-leaflet | MailboxListPage.tsx je importala komponentu MailboxLocationForm.tsx koja je sadržavala import `react-leaflet` (nije u package.json dependencies). Ovo je uzrokovalo React runtime crash kada bi komponenta trebala biti renderirana, što je sprečilo mapu da se otvori. Problem: dva primjerka iste datoteke na različitim lokacijama (src/application/models/ i src/ui/components/Mailbox/), ali oba koriste neispravne importove. | Zadržati obe kopije i pokušati da popravim react-leaflet kod; obrisati obe kopije jer je MailboxForm.tsx bolji pristup; refaktor MailboxLocationForm da koristi direktan Leaflet API umjesto react-leaflet | Obrisati obe kopije MailboxLocationForm.tsx; MailboxListPage.tsx koristi MailboxForm.tsx koji koristi direktan Leaflet API (funkcionalan kod) | MailboxForm.tsx je već ispravno implementiran i koristi direktan Leaflet API bez react-leaflet zavisnosti. Dva primjerka MailboxLocationForm su dead code ili greška pri kopiranju. Čišće rješenje: ukloniti dead code i koristiti jednu ispravnu komponentu. Dodatni bonus: `npm run build` sada prolazi bez grešaka. | Ako je negdje u kodu bio import iz MailboxLocationForm, build bi počeo da pada — ali pošto je samo MailboxListPage.tsx koristila (i ta je ažurirana), nema breaking changesa. | Aktivna |
| DEC-024 | 2026-05-06 | Strukturirati /admin/mailboxes da prikaže formu i listu na istoj stranici | Korisnik je želio da forma bude na /admin/mailboxes (glavnoj stranici) umjesto /admin/mailboxes/new. Pitanje: gdje smjestiti formu — integrisati u listu ili zadržati zaseban /new URL? | Zadržati /admin/mailboxes/new i /admin/mailboxes kao zaseban endpoint; integrisati formu kao collapsible sekciju na /admin/mailboxes stranici; preseliti listu na /admin/mailboxes/new | Integrisati formu kao toggle-able sekciju na /admin/mailboxes (MailboxListPage.tsx); sačuvati /admin/mailboxes/new kao alternativnu rutu (CreateMailboxPage.tsx) | Korisniku je intuitivnije imati sve na glavnoj stranici — forma + lista. Toggle omogućava pregledniji prikaz. /admin/mailboxes/new ostaje dostupna ako korisnik želi direktnu formu bez liste. Pogled je sličan MailboxListPage s button-om "+Dodaj novi sandučić". | Ako korisnik koristi direktnu rutu /admin/mailboxes/new, vidjeće samo formu. Obrnuto, na /admin/mailboxes može vidjeti listu i formu. Nema problema jer su oba scenarija korisni. | Aktivna |
| DEC-025 | 2026-05-06 | TypeScript enum rješenje za MailboxType | TypeScript enum `enum MailboxType` nije bio kompatibilan sa `erasableSyntaxOnly` podešavanjem u tsconfig.json, što je uzrokovalo build greške. | Ukloniti `erasableSyntaxOnly`; koristiti `const enum`; koristiti `const + as const` pattern | `const MailboxType = {...} as const` + `type MailboxType = typeof MailboxType[keyof typeof MailboxType]` | `const + as const` pattern omogućava type-safe enum bez potrebe za `enum` keyword, kompatibilan sa svim TypeScript podešavanjima. Daje bolju tree-shaking podršku i manji bundle size. | Svi budući enumovi u projektu trebaju koristiti isti pattern za konzistentnost. Potrebno je kreirati i label mapping objekt za prikaz u UI. | Aktivna |
| DEC-026 | 2026-05-07 | Priority i Status kao zasebna polja entiteta umjesto izvedeni iz Type | AC za PBI-019 zahtijeva kolone Prioritet (Visok/Srednji/Nizak) i Status (Prazan/Pun) ali ti pojmovi nisu postojali u Mailbox entitetu. Pitanje: izvesti ih iz postojećeg Type polja (npr. Type=SpecialPriority → Prioritet=Visok) ili dodati zasebna polja u entitet? | Izvesti vrijednosti iz Type polja (read-only kompozicija); dodati Priority i Status kao zasebne kolone u DB | Dodati Priority i Status kao zasebne MailboxPriority/MailboxStatus enume i polja na entitetu | PBI-018 (Ibrahimov rad — uređivanje sandučića) eksplicitno spominje Prioritet kao polje koje se mijenja, što znači da mora biti zaseban editabilan property. Status (Prazan/Pun) će se kasnije ažurirati kroz proces pražnjavanja sandučića. Izvedeno polje ne zadovoljava ove buduće potrebe. | Migracija dodaje dvije int kolone u Mailboxes tablu. Postojeći sandučići SpecialPriority tipa dobijaju Priority=Visok kroz SQL UPDATE u Up() metodi; ostali Srednji. Status=Prazan default. | Aktivna |
| DEC-027 | 2026-05-07 | Server-side paginacija/filteri/sort umjesto client-side | AC traži straničenje 25/str, filtere i sortiranje. Pitanje: sve raditi na frontendu (povući sve sandučiće odjednom) ili na backendu kroz query parametre? | Client-side: GET /api/mailboxes vraća sve, frontend filtrira/paginira; server-side: backend prima query parametre i vraća već paginirane podatke | Server-side: GET /api/mailboxes?page=&pageSize=&type=&priority=&search=&sortByPriority= vraća PagedResponse&lt;T&gt; | Skalabilnost — sa rastom baze sandučića client-side bi povlačio sve po svakom učitavanju stranice. Server-side filteri se mogu indeksirati kasnije. Clamping pageSize na 100 sprječava DoS. | Frontend mora pratiti page state i ponovo pozvati API na svaku promjenu filtera. Reset stranice na 1 pri promjeni filtera/sorta. | Aktivna |
| DEC-028 | 2026-05-07 | Sortiranje po prioritetu kao toggle umjesto asc/desc cikla | AC kaže "klik na zaglavlje kolone Prioritet, najhitniji prvi". Klasičan pattern u tabelama je da klik prebacuje između asc/desc/none kroz tri stanja. Pitanje: implementirati klasičan ciklus ili minimalan toggle? | Toggle on/off (jedan klik uključuje sort po prioritetu, ponovi isključuje); klasični asc/desc cikl; samo asc bez načina za isključenje | Toggle on/off. Default sort (off) je po SerialNumber. | AC ne traži desc smjer — "najhitniji prvi" je jasno asc po vrijednosti enum-a (Visok=1). Strogo poštivanje "ne implementirati ništa što nije u dokumentaciji". Toggle je jednostavniji i pokriva tačno traženo. | Klasičan tabelski UX bi imao 3 stanja po kolonama. Ako AC bude proširen, prelazak je jednostavan — samo dodati `sortDirection` state. | Aktivna |
| DEC-029 | 2026-05-07 | Modal mape koristi react-leaflet direktno umjesto OpenStreetMapPicker | AC traži klik na adresu da otvori modal sa lokacijom na mapi (read-only prikaz). Postoji OpenStreetMapPicker komponenta ali nema read-only mod (marker je draggable, footer kaže "Kliknite na mapu..."). Pitanje: dodati read-only prop u OpenStreetMapPicker (mijenja tuđi rad) ili koristiti react-leaflet direktno u modalu? | Dodati `readOnly` prop u OpenStreetMapPicker; koristiti OpenStreetMapPicker s no-op handlerom (marker i tekst i dalje pogrešni); MapContainer + Marker direktno u modalu bez click handlera | MapContainer + TileLayer + Marker direktno u MapModal komponenti | Minimalna izmjena u MailboxListPage, bez diranja OpenStreetMapPicker komponente koju koriste i drugi (CreateMailboxPage, inline forma). React-leaflet je već dependency. | OpenStreetMapPicker zadržava svoju jedinu odgovornost (interactive picker). MapModal je usko fokusiran na read-only prikaz. | Aktivna |
| DEC-030 | 2026-05-07 | Cookie-based authentication umjesto session-based za PBI-018 | 401 Unauthorized greška kod ažuriranja sandučića. ASP.NET Core nije prepoznavao session kao validni authentication. | Sesija-based authentication, JWT token authentication, Cookie-based authentication | Cookie-based authentication sa `UseAuthentication()` middleware | ASP.NET Core standardni authentication sistem koristi cookie-based authentication. Session nije automatski prepoznat kao validni authentication, dok cookie kreira `User.Identity.IsAuthenticated = true`. | Svi budući API endpointovi će raditi sa cookie-based authentication. Potrebno je osigurati cross-origin cookie konfiguraciju za development. | Aktivna |
| DEC-031 | 2026-05-07 | Read-only serijski broj u edit formi | PBI-018 zahtijeva edit formu sa prepopuliranim podacima. Pitanje: da li serijski broj treba biti editabilan? | Editabilni serijski broj sa validacijom jedinstvenosti, read-only serijski broj, sakriveno polje | Read-only serijski broj sa vizuelnom indikacijom | Serijski broj je jedinstveni identifikator sandučića. Mijenjanje bi kreiralo nove probleme sa integritetom podataka i audit trail-om. Read-only pristup je jednostavniji i sigurniji. | Korisnik vidi serijski broj ali ga ne može mijenjati. Sve promjene se logiraju sa originalnim serijskim brojem. | Aktivna |
| DEC-032 | 2026-05-07 | Audit logging za sve promjene sandučića | PBI-018 zahtijeva audit log za praćenje promjena. Pitanje: kako implementirati audit logging? | Logovanje samo u text file, logovanje u bazu bez detalja, detaljan audit log sa svim poljima | MailboxAuditLog tabela sa detaljnim poljima (FieldName, OldValue, NewValue, UserId, Timestamp, Action) | Potpuna transparentnost svih promjena. Mogućnost praćenja ko je šta promijenio i kada. Podržava buduće analize i compliance zahteve. | Svaka promjena sandučića kreira jedan ili više zapisa u MailboxAuditLog tabeli. Povećava broj DB operacija. | Aktivna |
| DEC-033 | 2026-05-09 | Geocoding adrese kroz Nominatim API | Korisnik ne bi trebao ručno unositi Latitude/Longitude koordinate. Pitanje: koji servis koristiti za geocoding? | Google Maps API (plaćen), Nominatim OpenStreetMap (besplatan), HERE Maps (freemium) | Nominatim OpenStreetMap API | Besplatan, bez API ključa, kompatibilan s već korištenim OpenStreetMap tile serverom. Ograničenje: 1 request/sekundi, ali prihvatljivo za administratorsku upotrebu. | Adresa se automatski popunjava iz mape. Reverse geocoding pri kliku na mapu vraća punu adresu koja može biti predugačka — administrator treba moći ručno skratiti. | Aktivna |
| DEC-034 | 2026-05-09 | Forma za dodavanje sandučića kao zasebna stranica | Inline forma unutar liste sandučića bila je loš UX — forma je velika i prekida pregled liste. Pitanje: inline ili zasebna stranica? | Inline forma u listi (postojeće rješenje), modal dijalog, zasebna stranica /admin/mailboxes/new | Zasebna stranica /admin/mailboxes/new | Ruta već postoji u AppRouter, CreateMailboxPage je već kreiran i konzistentan s ostalim formama. Zasebna stranica daje više prostora za mapu i validacijske poruke. | Lista sandučića je čistija. Korisnik mora navigirati nazad nakon dodavanja — prihvatljivo jer se redirect radi automatski nakon uspješnog spašavanja. | Aktivna |
| DEC-035 | 2026-05-11 | Vremenska dostupnost sandučića kroz dva opcionalna termina + 24/7 flag | US-32 zahtijeva definisanje vremenskih okvira dostupnosti. Pitanje: jedan termin ili više, i kako modelirati 24/7? | Jedan termin po danu; dva termina po danu; niz termina (array) | Dva fiksna termina (Slot1Start/Slot1End, Slot2Start/Slot2End) + IsAlwaysAvailable boolean flag na entitetu | AC eksplicitno navodi maksimalno dva termina dnevno. Fiksna polja su jednostavnija od niza za validaciju i mapiranje. IsAlwaysAvailable flag kao posebno polje čini intent jasan i onemogućava dvosmislenost (null vs prazno). | Ako se pojavi potreba za više od dva termina, model zahtijeva migraciju. Prihvatljivo jer AC ne predviđa taj scenarij. | Aktivna |
| DEC-036 | 2026-05-12 | Road-aware prikaz generisane rute na mapi | Generisana ruta se na Leaflet mapi crtala pravolinijski između koordinata, bez praćenja cesta. Pitanje: koristiti custom polylines ili routing engine koji računa cestovnu trasu? | Ostatati na običnom Polyline prikazu, koristiti Leaflet Routing Machine s javnim OSRM routerom, razviti vlastiti backend routing servis | Leaflet Routing Machine s OSRM routerom | Najmanji rizik i najbrži način da ruta vizuelno prati ceste bez promjene backend ugovora. Postojeći `routeItems` i dalje ostaju izvor istine, a routing plugin samo poboljšava map vizualizaciju. | Frontend sada zavisi od javnog OSRM servisa za izračun trase. Ako se pokaže nestabilnim, kasnije se može zamijeniti vlastitim routing servisom ili proxyjem. | Aktivna |
| DEC-037 | 2026-05-12 | Sakrivanje routing itinerarija i sažimanje vremenskog prikaza | Leaflet Routing Machine po defaultu prikazuje itinerarij tekst preko mape, a backend vraća vremena sa sekundama. Pitanje: ostaviti default overlay ili ga ukloniti i formatirati vrijeme za UI? | Ostaviti default itinerary panel, premjestiti ga ispod mape, sakriti panel i zadržati samo liniju rute, backend mijenjati da šalje HH:MM | Sakriti itinerary panel na ovoj stranici i formatirati vremena na frontendu kao HH:MM | Mapa ostaje čista i čitljiva, a detaljna lista lokacija već postoji ispod mape. Formatiranje na UI-u je dovoljno jer backend i dalje koristi puni time string za internu obradu. | Nema promjene backend kontrakta; samo frontend prikaz. Ako se kasnije želi drugačiji format, može se lako prilagoditi helper funkcija. | Aktivna |
| DEC-038 | 2026-05-12 | Responsive proširenje admin lista umjesto uske centralne kolone | Lista sandučića i lista korisnika bile su prikazane u uskom centralnom card-u sa previše praznog prostora po stranama. Pitanje: zadržati kompaktan card ili proširiti na puni content width? | Zadržati `max-width` na form-cardu, proširiti samo tabele bez wrappera, napraviti posebne full-width shell kontejnere za admin liste | Full-width shell za admin liste, uz zadržavanje standardnog form-card stila za ostale forme | Najmanji zahvat koji popravlja odnos sadržaja i praznog prostora na desktopu, a ne dira login/form screens koji su namjerno kompaktniji. | Admin liste sada bolje koriste prostor unutar bijelog sadržaja; ostale stranice zadržavaju postojeću širinu i dizajn. | Aktivna |
| DEC-039 | 2026-05-13 | Modeliranje radnih dana sandučića kao bit flag enum sa Zod validacijom | US-33 zahtijeva da administrator označi dane u sedmici kada je sandučić dostupan (minimum jedan dan). Pitanje: kako modelirati — niz od 7 boolean-a, sedam odvojenih polja, ili bit flag enum sa kombinacijama? | Niz od 7 boolean-a (`WorkingDays: [true, true, ..., false]`), odvojena polja za svaki dan (Monday, Tuesday, ...), bit flag enum sa kombinacijama (None=0, Ponedjeljak=1, RadniDani=31, SvakiDan=127) | Bit flag enum sa kombinacijama (`MailboxWorkingDays` na backendu i frontendu kao `const + as const`) | Kompaktan na bazi (jedan int umjesto niza ili 7 polja), podrška ugrađenih kombinacija (RadniDani=Mon-Fri, SvakiDan=All), direktna bitwise operacija za validaciju "barem jedan dan" (workingDays != 0). Frontend UI sa 7 checkboxa mapira XOR toggle logiku na ključne bitse. Zod validacija na frontendu i service-layer na backendu. | Bit flag model je kompatibilan sa postojećom Mailbox tablelom; nema dodatnih kolona. Validacija na backendu je minimalna (`workingDays != MailboxWorkingDays.None`). Budućnost: ako se pojavi potreba za specifičnim danima (npr. "nikad nedjeljom"), model fleksibilan — samo logika se proširuje. Default za nove sandučiće: RadniDani (Mon-Fri). | Aktivna |
| DEC-040 | 2026-05-17 | Mapiranje DayOfWeek → MailboxWorkingDays kroz switch expression | Pri implementaciji working days filtera u `RouteService.GenerateRouteAsync`, potrebno je znati koji bit `MailboxWorkingDays` odgovara danu rute. .NET `DayOfWeek` enum (Sunday=0, Monday=1, ..., Saturday=6) ne poklapa se numerički s `MailboxWorkingDays` flags enumom (Ponedjeljak=1, Utorak=2, Srijeda=4, ..., Nedjelja=64). Pitanje: kako izvršiti konverziju bez grešaka? | Direktan cast `(MailboxWorkingDays)date.DayOfWeek` (netačno — vrijednosti ne poklapaju se); dictionary `Dictionary<DayOfWeek, MailboxWorkingDays>` inicijalizovan statički; switch expression kao private static metoda | `private static MailboxWorkingDays ToDayFlag(DateOnly date)` switch expression koji eksplicitno mapira svaki `DayOfWeek` na odgovarajući `MailboxWorkingDays` bit | Direktan cast bi vratio pogrešne bitove (npr. `DayOfWeek.Sunday=0` bio bi `None`, a ne `Nedjelja=64`). Switch expression je čitljiv, type-safe, i C# compiler upozorava ako novi `DayOfWeek` slučaj nije pokriven. Dictionary pristup je funkcionalno ekvivalentan ali nema compiler-time zaštitu. | Svaka buduća izmjena `MailboxWorkingDays` enum-a (npr. dodavanje novih dana) zahtijeva ažuriranje switch-a u `ToDayFlag`. Metoda je private static — bez externih zavisnosti, lako testirati direktno ili kroz `GenerateRouteAsync`. | Aktivna |
| DEC-041 | 2026-05-20 | Audit zapis izmjena redoslijeda — polja na Route entitetu vs. zasebna tabela | AC za PBI-025 zahtijeva da sve izmjene redoslijeda budu evidentirane u bazi s vremenskim pečatom i imenom dispečera. Pitanje: kreirati zasebnu `RouteReorderLog` tabelu (jedan zapis po kliku) ili dodati polja `LastReorderedAt`/`LastReorderedBy` direktno na `Route` entitet (jedan zapis po čuvanju)? | Zasebna `RouteReorderLog` tabela s jednim redom po svakom kliku dugmeta; zasebna tabela s jednim redom po čuvanju; polja `LastReorderedAt`/`LastReorderedBy` direktno na `Route` | Polja `LastReorderedAt` i `LastReorderedBy` na `Route` entitetu, ažuriraju se pri svakom pozivu `PUT /api/routes/{id}/reorder` | Dispečer može kliknuti ↑/↓ desetak puta prije čuvanja — zapis po svakom kliku nije smislen za reviziju i punio bi tabelu šumom. Relevantan trenutak za reviziju je kada je izmjena sačuvana i postala aktivna. Dva polja na postojećem entitetu ne zahtijevaju novu tabelu ni migraciju zasebnog entiteta — manji zahvat za isti revizorski cilj. | Čuva se samo zadnji čin čuvanja, ne puna historija. Prihvatljivo za operativnu reviziju u MVP fazi. Ako se pojavi potreba za punom historijom izmjena, može se naknadno dodati `RouteReorderLog` tabela. | Aktivna |
| DEC-042 | 2026-05-20 | Dvostruka kalkulacija vremena dolaska — client-side za live UX + server-side pri čuvanju | AC zahtijeva da se procijenjeno vrijeme dolaska ažurira odmah nakon svakog pomaka i da se konačne vrijednosti trajno pohrane. Pitanje: kalkulacija samo na serveru (zahtijeva round-trip za svaki pomak) ili samo na klijentu (baza ostaje neažurirana), ili oboje? | Samo server-side: svaki klik ↑/↓ poziva API (sporo, previše zahtjeva); samo client-side: baza nikad ne dobija ažurirana vremena; dvostruko: frontend računa lokalno pri svakom pomaku, backend ponovo računa i čuva pri "Sačuvaj" | Dvostruka kalkulacija: `recalculateArrivals()` TypeScript helper u `GenerateRoutePage.tsx` za instant prikaz pri svakom pomaku; `RouteService.ReorderRouteAsync()` ponovo izračunava i čuva pri eksplicitnom čuvanju | Ista Euklidska formula i iste konstante (`SPEED_KMH=30`, `STOP_MINUTES=5`, `DEPOT_LAT/LNG`) implementirane na obje strane osiguravaju identične rezultate. Korisniku su vremena vidljiva odmah (dobra UX responsivnost), a server je uvijek izvor istine pri čuvanju — nema rizika od desinhronizacije jer server ponovo izračuna iz koordinata, ne uzimajući klijentske vrijednosti kao input. | Isti algoritam mora biti sinhronizovan na dvije strane koda. Promjena konstanti (brzina, trajanje zaustavljanja) zahtijeva ažuriranje i TypeScript helpera i C# metode. Označiti konstantne vrijednosti u oba fajla da se lakše pronalaze. | Aktivna |
| DEC-043 | 2026-05-20 | `useRef` za čuvanje originalnog redoslijeda umjesto zasebnog `useState` | Frontend treba zapamtiti originalni redoslijed stavki (onaj koji je algoritam generisao) kako bi "Resetuj" dugme i ✎ ikonica radili ispravno. Pitanje: čuvati original kao `useState<RouteItemResponse[]>` ili `useRef<RouteItemResponse[]>`? | `useState` — React state koji trigerira re-render pri promjeni; `useRef` — mutabilna kutija bez utjecaja na render ciklus; vanjska varijabla unutar modula (ne-reaktivna) | `useRef<RouteItemResponse[]>` inicijalizovan na `[]`, popunjen pri svakom novom generisanju rute | Originalni redoslijed se nikad ne smije promijeniti tokom interakcije dispečera — mijenja se samo kada se generira nova ruta ili kada se čuvaju izmjene (u oba slučaja i `localItems` dobija novu vrijednost). `useState` bi trigerao nepotreban re-render svaki put kad se originalni niz postavi. Semantički, `useRef` je ispravno — ovo je imperativna referentna vrijednost, ne dio reaktivnog toka podataka koji određuje izgled UI-ja. | `originalItemsRef.current` se čita direktno u `moveItem()` i `resetToOriginal()` bez potrebe za deklaracijom u dependency arrayu `useEffect`/`useMemo`. Ako se zaboravi ažurirati `originalItemsRef.current` pri čuvanju izmjena, reset dugme bi pokazalo stari originalni redoslijed umjesto novo sačuvanog. | Aktivna |
| DEC-044 | 2026-05-20 | Definicija aktivnog poštara za dodjelu rute | US-23 traži listu svih aktivnih poštara, ali `User` entitet trenutno nema zaseban `IsActive` flag; postoji samo `Role` i `IsLockedOut`. Pitanje: da li dodati novu kolonu ili mapirati aktivnost na postojeće stanje korisnika? | Dodati `IsActive` kolonu na `User`; koristiti sve korisnike sa ulogom `PostalWorker`; koristiti samo `PostalWorker` korisnike koji nisu zaključani (`IsLockedOut=false`) | Aktivni poštar = `Role == PostalWorker` i `IsLockedOut == false` | Najmanji zahvat koji zadovoljava AC bez uvođenja nove administracije statusa korisnika. Zaključan nalog ionako ne smije koristiti sistem, pa nije operativno dostupan za dodjelu rute. | Ako se kasnije uvede zasebno deaktiviranje zaposlenika bez lockout-a, treba dodati `IsActive` polje i ažurirati `RouteService.GetAvailablePostmenAsync`. | Aktivna |
| DEC-045 | 2026-05-20 | Uključiti `PostRoute.Api.Tests` u solution | API test projekat je postojao, ali nije bio dio `PostRoute.sln`, pa `dotnet test .\PostRoute.sln` nije pokretao controller testove. Pitanje: ostaviti ga odvojeno ili uključiti u standardni test tok? | Ostaviti odvojeno; uključiti u solution; obrisati API test projekat | Dodati `tests/PostRoute.Api.Tests/PostRoute.Api.Tests.csproj` u `PostRoute.sln` | Controller ugovor je bitan za PBI-023 (`assign` i `available-postmen` endpointi), pa standardna komanda mora pokrivati API sloj. Ovo smanjuje rizik da API testovi zastare neprimijećeno. | `dotnet test .\PostRoute.sln` sada pokreće DAL, BLL i API testove. Postoji transitive EF Core Relational verzijsko upozorenje u API test buildu, ali testovi prolaze. | Aktivna |
| DEC-046 | 2026-05-23 | Identifikacija poštara pri dohvatanju današnje rute (PBI-026) | US-26 zahtijeva da poštar vidi samo svoju dodijeljenu rutu za tekući dan. Pitanje: kako sigurno utvrditi identitet poštara u backendu? | Proslijediti `postmanId` kao parametar rute (`/api/routes/postman/{id}/today`); izdvojiti `postmanId` direktno iz JWT tokena na serveru | Izdvajanje `postmanId` iz JWT tokena (`ClaimTypes.NameIdentifier`) unutar kontrolera | Sigurnost — sprečava IDOR (Insecure Direct Object Reference) ranjivost. Da se ID šalje kroz URL, jedan poštar bi mogao pregledati rutu drugog poštara mijenjajući ID. Čitanje iz tokena garantuje autentičnost zahtjeva. | API endpoint ne prima ID parametar (`GET /api/routes/my-assigned-route/today`). Frontend ne mora proslijeđivati ID poštara u URL-u. | Aktivna |
| DEC-047 | 2026-05-23 | Odgovor servera kada poštar nema dodijeljenu rutu (PBI-026) | Kada poštar zatraži svoju današnju rutu, moguće je da mu dispečer još nije dodijelio nijednu. Pitanje: koji HTTP status kod i format odgovora vratiti u tom slučaju? | Vratiti `404 Not Found`; vratiti `204 No Content`; vratiti `200 OK` sa specijalnom porukom | Vratiti `200 OK` sa JSON objektom `{ Message: "Nema dodijeljene rute za danas." }` | Nedostatak dodijeljene rute je validno, očekivano poslovno stanje (pogotovo na početku dana), a ne "greška" u smislu nedostajućeg resursa. Bacanje 404 bi na frontendu okidalo globalne error handlere. | Frontend API klijent provjerava postojanje `message` polja u odgovoru i vraća `null` vrijednost umjesto bacanja izuzetka, čime se lakše prikazuje "empty state" na UI-u. | Aktivna |
| DEC-048 | 2026-05-23 | Proširenje `MailboxStatus` enum-a bez DB migracije (PBI-027) | US-27 zahtijeva tri nova statusa sandučića (`Obrađen`, `Napunjen`, `Ispraznjen`). `MailboxStatus` enum već postoji s dva statusa (`Prazan=0`, `Pun=1`). Pitanje: da li dodavanje novih enum vrijednosti zahtijeva EF Core migraciju? | Kreirati novu migraciju; direktno dodati vrijednosti u enum bez migracije | Direktno dodati `Obraen=2`, `Napunjen=3`, `Ispraznjen=4` u enum bez DB migracije | PostgreSQL čuva `MailboxStatus` kao `integer` kolonu (ne kao nativni `ENUM` tip), a EF Core mapira C# enum na svoju `int` vrijednost. Dodavanje novih enum vrijednosti samo proširuje raspon validnih integera — shema u bazi se ne mijenja. | Nema migracije niti downtime-a. Ako bi se u budućnosti uveo nativni PostgreSQL ENUM tip, tada bi migracija bila neophodna. C# naziv je `Obraen` (bez dijakritičkog znaka) jer C# identifikatori ne smiju sadržavati `đ`. | Aktivna |
| DEC-049 | 2026-05-23 | Session-based identifikacija korisnika pri ažuriranju statusa sandučića (PBI-027) | `PATCH /api/mailboxes/{id}/status` treba znati ko mijenja status radi audit loga. `MailboxesController` koristi custom session-based auth (ne `[Authorize]` s JWT kao `RoutesController`). Pitanje: čitati ID korisnika iz sesije ili uvesti `UserId` kao parametar zahtjeva? | Primiti `UserId` kroz tijelo zahtjeva; čitati iz JWT tokena (`ClaimsPrincipal`); čitati iz sesije (`HttpContext.Session`) | Čitati `UserId` iz `HttpContext.Session.TryGetValue("UserId", ...)`, vratiti 401 ako nedostaje ili nije validan `Guid` | Konzistentno s ostalim `MailboxesController` endpointima koji koriste isti sesijski mehanizam. Primanje `UserId` kroz tijelo zahtjeva je sigurnosni propust — pozivatelj bi mogao lažirati identitet. JWT pristup bi zahtijevao refaktoring auth mehanizma za cijeli `MailboxesController`. | Endpoint ne koristi `[RequiredRole]` atribut — dovoljna je provjera prisustva valjane sesije. Korisnik koji nema sesiju dobija 401, što je konzistentno s ostalim endpointima. | Aktivna |
| DEC-050 | 2026-05-23 | `TestSession : ISession` helper klasa umjesto Moq za mockiranje sesije u controller testovima (PBI-027) | Controller testovi za `UpdateStatusAsync` trebaju simulirati `HttpContext.Session` s različitim stanjima (validan `UserId`, nema `UserId`, neispravan `UserId`). `ISession` ima `TryGetValue` s `out` parametrom što je komplikovano za mockiranje s Moq-om. | Moq s `out` parametrima (komplikovana setup sintaksa: `Setup(s => s.TryGetValue(..., out It.Ref<byte[]>.IsAny)).Returns(...)` + ručno serializovanje); kreirati `TestSession : ISession` koji koristi `Dictionary<string, byte[]>` interno; koristiti `FakeItEasy` biblioteku | Kreirati `private sealed class TestSession : ISession` unutar test klase s `Dictionary<string, byte[]>` kao internom pohranom | Čista i razumljiva implementacija bez Moq out-parameter sintaksnog overhead-a. `SetString` i `TryGetValue` ponašaju se identično kao prava sesija. Testovi su čitljivi — jasno je šta sesija sadrži. Ne uvodi nove test zavisnosti. | `TestSession` je privatna klasa unutar test fajla — dostupna samo tom test klasom. Ako se pattern ponovi u drugim test klasama, može se premjestiti u shared test helper. | Aktivna |
| DEC-051 | 2026-05-23 | Real-time ažuriranje statusa sandučića kroz React lokalni state (PBI-027) | US-27 zahtijeva "real-time ažuriranje" statusa sandučića u prikazu rute nakon što poštar označi status. Pitanje: implementirati WebSocket/SignalR vezu ili ažurirati lokalni React state? | WebSocket/SignalR za push notifikacije s backend-a; polling svakih N sekundi; ažuriranje lokalnog React statea odmah nakon uspješnog PATCH poziva | Optimistično ažuriranje lokalnog React statea: nakon uspješnog `PATCH /api/mailboxes/{id}/status`, `setRoute()` mapira stavke i mijenja `mailboxStatus` za odgovarajući `mailboxId` | "Real-time" u kontekstu US-27 odnosi se na to da poštar odmah vidi promjenu u UI-u bez ponovnog učitavanja stranice — ne na sinhronizaciju između više korisnika. Poštar je jedina osoba koja koristi tu stranicu u tom trenutku. WebSocket/SignalR bi uveo infrastrukturnu kompleksnost nerazmjernu stvarnoj potrebi. | Ako dispečer promijeni rutu dok je poštar na terenu, poštar ne vidi tu promjenu bez ručnog osvježavanja. Prihvatljivo za MVP fazu jer dispečer ne mijenja rute u toku dana. | Aktivna |
---
**Statusi odluka:**
- **Aktivna:** Odluka je trenutno na snazi.
- **Izmijenjena:** Odluka je pretrpjela manje promjene.
- **Zamijenjena:** Odluka više ne vrijedi jer ju je zamijenila nova (navesti ID nove odluke).
